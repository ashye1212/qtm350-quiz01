List of all commands used in this project
  674  git add reports/
  675  git status
  676  git commit -m "Add reports directory"
  683  git add . && git commit -m "Update project files"
  684  touch .gitignore
  685  echo -e "temp/\ndataset0?.csv" > .gitignore
  686  cat .gitignore
  687  git add .gitignore && git commit -m "Add .gitignore file"
  688  git checkout main\ngit merge feature-update
